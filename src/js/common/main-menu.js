/**
 * Set up main menu.
 *
 * @param { string } msg Message to console log.
 */
const mainMenu = (msg = 'main-menu.js loaded') => {
    console.log(`🤞 ${msg}`) // eslint-disable-line no-console
}

// run it!
mainMenu()
