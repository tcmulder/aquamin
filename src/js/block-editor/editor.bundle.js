/**
 * Include the block editor scripts
 *
 * This allows the build tool to focus on a single directory,
 * while keeping blocks in a separate, well-organized folder.
 */
import '../../../blocks/editor';
